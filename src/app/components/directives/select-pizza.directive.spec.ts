import { SelectPizzaDirective } from './select-pizza.directive';

describe('SelectPizzaDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectPizzaDirective();
    expect(directive).toBeTruthy();
  });
});
